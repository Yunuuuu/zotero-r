% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key.R
\name{zotero_autho}
\alias{zotero_autho}
\title{Zotero API Key Management and OAuth Authorization}
\usage{
zotero_autho(api_key = NULL, oauth_strategy = NULL, oauth_userid = NULL)
}
\arguments{
\item{api_key}{Optional API key. If provided, the function will set the API
key in the memory and return without performing OAuth authorization.}

\item{oauth_strategy}{A character string specifying how to access the OAuth
token and whether to cache it:
\itemize{
\item \code{"read"}: Only read from the cached OAuth token file. Will error if the
cached file does not exist.
\item \code{"auth"}: Perform OAuth authorization without caching the token.
\item \code{"save"}: Perform OAuth authorization and cache the token locally for
future use.
}

If \code{oauth_strategy} is \code{NULL} (the default), the function will attempt to:
\itemize{
\item First attempt to read from a cached OAuth token if available.
\item If the cached token is not found, it will attempt to perform OAuth
authorization.
}}

\item{oauth_userid}{Optional user ID. Used to retrieve a cached OAuth token
specific to the user. If provided, the function will search for a cached
token associated with this user ID. If not provided, the function will use
the last used token key file or the most recently available cached token.}
}
\description{
This function manages API key handling and OAuth authorization for the Zotero
API. It supports:
\itemize{
\item Caching OAuth tokens for future use.
\item Retrieving existing tokens from cached files.
\item Performing new OAuth authorization flows.
}
}
